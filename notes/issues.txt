
* Add validating for C++/Qt declaration fields.

* Overhaul the C++/Qt type system so it is visible in the block system, make two new block types similar to enumerations but for types and have the special type combo box populate its list from that.

* Move all C++/Qt function properties to the function block (like explicit) and change the flags in the name to be surrounded by [...], also changing explicit and default to have one letter flags.

