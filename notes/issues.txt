
* Add the ability for application to update the current working directory when a project is opened/saved.

* Make the C++/Qt function view hide the steps of operation by default.

* Fix newly created C++/Qt blocks having invalid blank values.

* Fix C++/Qt bug where global header file not being parsed.

