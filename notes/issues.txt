
* Make it so templates and virtual functions do not mix in C++/Qt.

* Make qt object and templates mutually exclusive in C++/Qt.

* Make it so you cannot add signals/slots if class is not qt object in C++/Qt.

* Make all the C++/Qt edit dialogs remember their size.

* Make a settings menu which will currently just have C++/Qt for things like indent size, remembering it all in QSettings.

* Fix C++/Qt parsing bug where if destructor comes before constuctor then mismatch of functions occurs.

* Add friend and using declarations for classes in C++/Qt.

* Fix C++/Qt parse formatting of header files at end. (missing spaces)

* Fix C++/Qt bug where variable initializers are not present if normal class members.

* Fix C++/Qt parsing bug where enumerations do not write and ending semicolon.

* Fix C++/Qt class bug where qt object checkbox has no name.

* Add indented definition of initializer lists for variables in C++/Qt parser if it exceeds a 100 lines.

