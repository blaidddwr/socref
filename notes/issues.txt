
* Make all the C++/Qt edit dialogs remember their size.

* Make a settings menu which will currently just have C++/Qt for things like indent size, remembering it all in QSettings.

* Add friend and using declarations for classes in C++/Qt.

* Make class names shorter in context of templates and inheritance.

* Make function names shorter in context of templates and trimming the return to ... if larger than a certain length. Also remove the arguments and change it to the number of arguments.

* If updating a block fails post some kind of message about it.

* Add mutable property for C++/Qt variables.

* Fix C++/Qt parsing bug where paranthesis in comments is mistaken as a function header.

* Fix C++/Qt parsing bug where template classes are added to global headers.

* Add default property for C++/Qt constructors, destructors, and method operators.

